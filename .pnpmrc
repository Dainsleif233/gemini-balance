# pnpm 配置文件

# 启用严格的对等依赖检查
strict-peer-dependencies=false

# 自动安装对等依赖
auto-install-peers=true

# 使用符号链接（默认行为，但明确指定）
symlink=true

# 启用 shamefully-hoist 以提高兼容性（如果需要）
# shamefully-hoist=true

# 设置存储目录（可选，使用默认值）
# store-dir=~/.pnpm-store